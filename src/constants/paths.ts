import path from 'path';

export const root = path.resolve(__dirname, '..', '..');
export const src = path.join(root, 'src');
export const days = path.join(src, 'days');
export const template = path.join(src, 'template');
